/target
.vercel

# Ephemeral review/staging outputs (committed baselines live in
# tests/visual/baselines; tests/vendor holds the reviewed vendor copies)
.review/

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads runtime/credential files (bd doctor required patterns)
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
