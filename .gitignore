/target
.vercel

# PixelProof ephemeral outputs (committed baselines live in tests/visual/baselines)
.review/visual-renders/
.review/visual-diffs/

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads runtime/credential files (bd doctor required patterns)
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
