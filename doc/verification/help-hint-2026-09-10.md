# HelpHint verification - 2026-09-10

Bead: `ldui-q25n`

## Scope

This evidence covers the reusable `HelpHint` composition, its Tooltip demo,
pure disclosure state, and its dedicated release-WASM browser lane. Consumer
migration, publication, deployment, and consumer baseline acceptance remain
outside this change.

## Test-first evidence

The initial compile-only RED established that the new public module did not
exist. A minimal state scaffold then produced the behavioral RED:

```text
cargo test -p leptos-daisyui-rs --lib --features test-mode help_hint
test result: FAILED. 1 passed; 5 failed
```

The failures were the intended behavior assertions for transient hover/focus,
pointer pin persistence, keyboard-pin blur, second activation, and the derived
description ID. After the minimal state implementation the same command passed
6/6. A second RED isolated the multiple-instance stack regression:

```text
cargo test -p leptos-daisyui-rs --lib --features test-mode hidden_or_unmounted_active_hint_reveals_previous_visible_hint
test result: FAILED. 0 passed; 1 failed
```

The final focused state run passed:

```text
cargo test -p leptos-daisyui-rs --lib --features test-mode help_hint
test result: ok. 7 passed; 0 failed
```

The initial browser run failed all three grouped journeys and exposed fixture
and harness defects: an offscreen-pointer setup error, the CDP wrapper dropping
the required empty `touchPoints`, and subject activation being intercepted by
an intentionally pinned screenshot state. These are not claimed as product
mutation controls; the behavioral RED evidence is the native state and stack
tests above. Review of the rendered state additionally found DaisyUI's blank
`::before` chrome, its 8 px tooltip dead gap, and centered clipping at the
pane's left edge. The final oracle scrolls before real input, preserves an
explicit empty touch array, checks both pseudo elements, crosses the real
trigger-to-description boundary, and checks rendered bounds.

## Final focused evidence

Test-only type check:

```text
cargo check -p leptos-daisyui-rs --test help_hint_smoke
Finished `dev` profile ... exit 0
```

Release browser lane:

```text
cargo xtask test-help-hint
Finished `release` profile [optimized] target(s) in 14m 29s
running 3 tests
test pointer_keyboard_escape_and_multiple_instances_are_truthful ... ok
test trusted_touch_pins_and_unmount_removes_the_global_listener ... ok
test variants_and_subject_keep_stable_accessible_semantics ... ok
test result: ok. 3 passed; 0 failed; 0 ignored
===== xtask summary =====
  PASS test-help-hint
1/1 passed
```

The lane rebuilt and stamped the stylesheet, served the release bundle on one
xtask-owned host, and stopped Trunk PID 57064 after the run. It proves:

- visible-label and compact trigger semantics, caller-derived and unique IDs,
  `aria-describedby`, `aria-expanded`, and a real `role="tooltip"` node;
- real pointer hover, movement through the zero-gap boundary into hoverable help,
  real keyboard focus/activation/Escape, and a computed focus-visible indicator;
- computed hidden state after Escape and second activation, with DaisyUI's
  `::before` and `::after` pseudo-elements suppressed locally;
- browser-trusted touch-derived clicks, including the first pin, second dismiss,
  and a third pin used for cleanup;
- pointer/touch pin persistence, keyboard-pin blur, internal subject/trigger
  focus transitions, reverse-registration multiple pins, and one Escape
  dismissing only the latest visible hint;
- no subject action during help interaction, ordinary subject activation when
  clicked directly, and no unrelated Escape side effect;
- removal of the exact capture listener added by a conditionally mounted hint;
- zero serious/critical scoped axe findings and no captured browser errors.

## Visual evidence

The focused lane saved `target/help-hint-q25n.png`. It was inspected at original
resolution. The visible-label and compact/subject variants are present, both
descriptions are fully readable, and their left edges and wrapping stay inside
the showcase content pane. This is review evidence, not a PixelProof baseline
refresh.

The only diagnostic in final commands was the workspace's existing
future-incompatibility warning for `proc-macro-error2 v2.0.1`.
