# Development log -- 2026

Dated feature waves and the findings behind them, relocated from
`CLAUDE.md` on 2026-09-15 to keep the always-loaded instructions lean.
Read a section when working near its components; promote a still-true
rule into `CLAUDE.md` rather than adding new dated waves here.

## Recent additions (2026-09-23, PersonPicker stale presence)

- **`PersonPicker::presence_as_of`** (ldui-8hmy, Office op-n66ib): the quiet
  replacement for a "presence may be out of date" banner -- dots grey in
  place, caption as tooltip and as the option's description. Details in
  `doc/plans/2026-09-21-person-picker-design.md`.
- The lane's first run failed on the TEST, not the product: its "caption must
  not leak into the option's name" probe read raw `textContent`, which
  includes the `aria-hidden` caption span by design -- the same trap
  `disabled_reason` set the day before. Compute a name as content minus
  `[aria-hidden="true"]` subtrees (the accname rule, and what `drift.js`
  does), never as `textContent`.

## Recent additions (2026-09-22 late, 02970ce: cursor footer + the p82h tail)

- **`ServerDataTable` cursor footer** (ldui-q14c): `ServerCursorPage` gains
  optional `total_rows` / `position` (`with_total_rows`, `with_position`,
  `known_range(displayed, page_size)` -> `ServerCursorRange`). The footer is
  now the ldui-5oce three-region `@container` grid in BOTH paging modes
  (`data-server-table-footer-controls` > label, `data-server-table-pagination`,
  `data-server-row-range`), so an offset and a cursor table on one page share
  one geometry. A CURRENT slice with both fields shows the `row_range` text
  and a single `aria-current="page"` slot between Previous/Next -- the one
  page a cursor can name; it never renders jumpable numbers it cannot honour.
  `ServerCursorTexts`/`DataTableTexts` deliberately unchanged: Office builds
  both as full struct literals ([per-type compatibility](../../CLAUDE.md)).
  Lane `test-server-cursor-footer`; its BREAK is the demo's "Forget total"
  toggle, which must collapse the range to the opaque caption while the pager
  does not move.
- **FilterBar Reset / Save-as-default** take `disabled_reason` -- the last two
  `disabled-without-reason` findings on `/components/client-snapshot-list`.
- **The trip `disabled_reason` causes in tests:** the reason is an
  `aria-hidden` `sr-only` span INSIDE the `<button>` (single root, for spread
  attributes), so raw `textContent` of a disabled button reads
  `"ResetNo active filters"` while the accessible name is still `"Reset"`.
  This repo's own filter-actions suite failed on it, and it is the first
  thing a consumer re-vendor will hit. Read the visible label by cloning and
  removing `[data-button-disabled-reason]`, and assert the reason through
  `aria-describedby` instead.

## Recent additions (2026-09-22 evening, eight Office-filed beads in one wave)

Four edit-only lanes in parallel, one serial compile, one gate run per fix
round (the backlog-fix-cycle rule); every browser lane that could reach the
change was run before closing anything.

- **`Button::disabled_reason: Signal<String>`** (ldui-p82h) -- blank is
  enabled; non-blank disables AND wires a hidden hint by `aria-describedby`
  plus `title`; `disabled=true` with no reason stamps
  `data-disabled-without-reason`. Not `Option<Signal<..>>`: Leptos's `into`
  path for an optional signal prop does not accept a plain `Option`, and one
  reactive string driving both state and copy is simpler anyway.
- **Audit rule 6 `disabled-without-reason`** (component-drift): name AND a
  resolving describedby. First run: 76 findings on the data-table page, 66
  of them pagers. The rule now exempts STATE-disabled controls by ancestry
  (`[data-pagination]`, `[aria-current=page]`, `.loading`/`aria-busy`,
  `aria-checked/pressed/selected=true`) -- a control at its boundary or
  already selected has nothing to explain -- and every remaining finding got
  a real reason (`KpiAction::disabled_reason`, `EntityTableTexts::reset_*_reason`,
  the ai-chat Translate hint, the showcase). Rulebook:
  `doc/visual-quality/disabled-without-reason.md`. Both pagers carry
  `data-pagination="true"` now (`Pagination`'s root, DataTable's controls,
  ServerDataTable's joins).
- **Quick-action row ruling** (ldui-q85o): ONE row -- LEFT Save Filter
  (always enabled; empty click is a no-op) then the badges (no "Filters:"
  caption; the group keeps `aria-label`), RIGHT `+ New` / consumer actions /
  the gear, which is now `EntityColumnChooserTrigger`'s default. It
  superseded, within the same evening, a `save_button_reason` field written
  an hour earlier; the field never shipped.
- **Row-action presets** (`RowActionButton`, ldui-bmqj) and
  **`EntityExportAction`** (ldui-e6x8) with the sprite glyphs; `download` is
  not in the Icon map yet, so Export references the sprite directly.
- **`EntityAutoFilters::from_columns`** (ldui-xgj8): the header is read live
  from the columns signal (ES pages no longer read "Filtrar Work type");
  **`filterable_text_debounced(ms)`** (ldui-ga96): server-apply text cells
  with `aria-busy` while pending, Enter applies at once, never an Apply
  button.
- **ClientCallWorkspace a11y** (ldui-tyyn) upstreamed by three-way merge
  against Office's re-vendor base (zero conflicts); **focus ring** (ldui-reod)
  animates offset, never colour -- and its browser proof had to disable the
  harness's `pp-freeze` sheet first, or the old CSS would have passed too.
- Test lessons: the ai-chat label set and the audit rule list are PINNED
  arrays, so adding a field/rule fails them by design; a thead has sort
  buttons, so "no Apply button" must be scoped to the filter row; the
  etl gate compiles this repo's LIVE tree, so an uncommitted audit rule
  reached a sibling's release gate before it landed here.

## Recent additions (2026-09-22, consumer beads: footer ruling + auto-filter copy)

- **EntityTable footer is one row, three regions** (ldui-5oce, owner ruling
  from 4iiz-Office production): rows-per-page LEFT, pager CENTERED under the
  table, "Showing x-y of z" RIGHT-justified on the same row -- the standard
  configuration, not a per-page option. The centering trick is a grid of
  `minmax(0,1fr) auto minmax(0,1fr)`: the `auto` track sits at the table's
  center whatever the flanks hold (the lane measured offset 0 for both the
  pager center and the summary's right edge). Below `@2xl` the three cannot
  fit -- at 390px the label overlapped the pager by 16px on the first run --
  so the pager drops to its own centered second row. Two test lessons: an
  `items-center` row means TOPS differ between a text span and a button
  group, compare centers; and a clipping negative control that expands by a
  fixed 64px silently stops reaching the region once a footer loses a row --
  expand past the measured slack instead.
- **`EntityAutoFilterTexts` substitutes `{column}` in `placeholder` and
  `all`** as `label` already did (ldui-v6ca, for 4iiz-etl's pixel-identical
  migration to `EntityAutoFilters`: "All phases", not "All"), through one
  `entity_auto_filter_text` helper.

## Recent additions (2026-09-19 to 21, PersonPicker + consumer-driven fixes)

`main` at `ab44fbb`. Gate 20/20, 3565 lib tests; browser lanes green with
counts verified (person-picker 3, filter-actions 2, client-snapshot 34).

- **PersonPicker** (`patterns/person_picker/`, design and plan in
  `doc/plans/2026-09-21-person-picker-*`, guide `doc/patterns/person-picker.md`):
  an opinionated person roster for 20+ pages generalising 4iiz-Office's
  `CoordinatorPanel`. A single-select **listbox** -- not a radiogroup, because
  radio semantics forbid the required deselect-by-second-click. Focus and
  selection are separate; the tab stop derives from the visible list so it is
  never unreachable. `PersonPresence::Unknown` ("not signed in") is distinct
  from `Offline` and has no dot -- Office's wire enum proved the first design's
  four states wrong. `PickerPerson::activity` carries what someone is DOING,
  never as a presence state. No per-page card markup, deliberately.
- **EntityTable four features from Office** (fd8e286): term-structured saved
  filters (additive; restore is absolute, not additive), reactive
  `with_filter_text_from`, `toolbar_leading`, a captioned badge row.
- **Saved filters**: own left-justified row out of the toolbar, "Filters:"
  caption at `/75` (was `/60`, failed AA), 5-badge display cap, 20-grapheme name
  cap, Save Filter disabled when there is nothing to save, apply button carries
  `data-pressable` (ldui-u2mx).
- **`SnapshotTablePage::side_panel`**; the column takes its content's width
  (8293082) -- 82ee866's fixed `w-80` overflowed a 360px panel.
- **FilterBar frame collapses when empty**, decided from rendered geometry:
  out-of-flow subtrees pruned whole (an OPEN dialog is visible and
  viewport-sized), leaves only (container height can be pure row-gap), write
  deferred with `request_animation_frame` (writing inside the observer resized
  the observed section and raised a ResizeObserver loop error).
- **Build size**: `target/` was 200.77 GB. `[profile.dev] debug =
  "line-tables-only"`, wasm incremental off, `cargo xtask clean-cache`.
- **Removed** the inherited Vercel deploy workflow and all Vercel config.

Findings worth keeping:

- `no_bare_library_buttons.rs` **excludes `entity_table/`**, which is how a
  bare button reached a consumer's audit. Widening it is open.
- Up to 78 sub-`/75` muted-text sites exist crate-wide; an audit is open.
- Consumers build some `*Texts` structs as full struct literals (Office's
  Spanish `EntitySavedFilterTexts`), so a new field there is a breaking change.

## Recent additions (2026-09-17, all-open-beads close-out)

Every open bead closed. `verify-full` 48/48 (37 suites, 4,010 tests) on
`68d8c3a`; `ccca279` followed with its own lane proof.

- **AI chat audits measured, not ratcheted** (ldui-iilm.11, ldui-q9zf). Both
  locale routes read internal 129 / typography 41 / depth 2 / drift 1 on the
  first sweep. Every one was fixed at source (list gaps raised to their rows'
  padding; the honesty grid to `gap-4`; the remember label to `gap-2`; the
  composer got a real `label for=`) or declared by role (`data-ai-chat-identifier`
  as a mono role; daisyUI's toggle resting shadow), ending at measured 0/0. The
  DropdownContent `<div>` change (ldui-afan) moved no other page's ceiling, but
  had left two `<li>` wrappers orphaned in the ai-chat settings popovers — an
  axe `listitem` violation only the merge surfaced.
- **Tool phase on messages** (ldui-ikvx, ldui-opnw): `MessageMeta.tool_phase`
  in ai-chat-core, `data-chat-tool-phase` on the transcript, and the lane
  selects the result by that hook, never by a literal `texts[3]`.
- **Pinned-field sweep** (ldui-zlu0): the hand-maintained list is gone; the
  suite sources are swept off disk at test time, with three anti-vacuity
  guards so the sweep cannot pass vacuously.
- **Helpdesk, from a consumer's findings** (ldui-efuf, ldui-8tlg, ldui-ftdy):
  `NewRequestDialog` focuses its summary on open (restore is the platform's —
  and a synthetic Escape never fires the native `cancel`, so lanes press a real
  key); `attachments=false` withholds the image field; and the host may hand in
  the whole `RoleCapabilities` table, because entitlements live in Jira, not
  in this crate.
- **KpiStrip help bubble measured, never indexed** (ldui-rzvv): see the new
  CLAUDE.md rule. The `ldui-k3ip` narrow-column contract had been red on main
  for a centred bubble on a row-end card, invisible to any index-based edge.
- **Live mode runbook executed** (ldui-9ics, ldui-iilm.10): connect, stream,
  Stop, server death and reload all observed as designed, including zero
  network on selecting Live and the mode never persisting.
- **Ollama on the desktop** (ldui-iilm.12) and the **AI Memory bridge**
  (ldui-qob6: editmark-server over MCP stdio, read-only by design, single
  principal stated on the wire) landed in editmark; both `feature/ollama-backend`
  branches were merged to `master` and retired.
- **`web-sys` features declared on the crate** with a source-scan guard
  (`test-web-sys-features`, cherry-picked from a stray branch and then formatted
  and linted): six features main had only been getting through sibling path
  deps — the shape that breaks a vendored copy.
- Process findings, now in memory: the pre-push hook runs the full native gate
  even for `git push --delete`; Claude Code's memory reaper kills background
  jobs at ~70% RAM (one heavy job at a time); a hand-launched trunk needs
  `NO_COLOR` unset and `CARGO_PROFILE_DEV_DEBUG=0`, which xtask does itself.

## Recent additions (2026-09-16, AI chat workspace showcase)

The `AiChatWorkspace` composite's showcase wave (ldui-iilm) closed with three
pieces worth keeping.

**A tool turn is two messages, and `data-chat-role` cannot tell them apart.**
`ai_chat_core` now stamps `MessageMeta::tool_phase`, and the transcript maps it
onto `data-chat-tool-phase` (`call` \| `result`) beside `data-chat-role`
(ldui-ikvx). Before that, a consumer wanting the tool RESULT had to reach for
it by document position, and a positional selector does not fail when the
transcript changes shape -- it starts describing something else. The attribute
is ABSENT, never empty, on a message with no phase: an empty value would still
match `[data-chat-tool-phase]` and make every ordinary bubble look like half of
a tool turn.

**A hand-maintained guard list has no way to notice what it is missing**
(ldui-zlu0). `editmark-core` parses with `ENABLE_SMART_PUNCTUATION`, so any
copy compared BYTE-EXACTLY against rendered markdown silently never matches if
it carries a quote, an apostrophe, `...` or `--`. The first version of the
guard cost an eight-minute browser lane to diagnose and was then protected by a
list of field names someone had to remember to extend -- and unlike
`FIELD_COUNT`, that list had no mechanical cross-check, so a later phase
pinning a new field would get no failure at all, just an assertion that
mysteriously never matched. The list is gone. The pins are now SWEPT from the
browser-suite sources at test time (`byte_pinned_fields` reads `tests/` off
disk rather than `include_str!`ing a path list that would rot the same way),
and the only thing stated by hand is a RENDER-PATH exemption in
`NON_MARKDOWN_PINS`. `knowledge_changed` is its founding member and its
exclusion stays deliberate: a `TranscriptAnnotation` renders through
`AnnotationRow`'s plain span, never through `MarkdownView`. Both directions are
guarded -- a swept pin with no exemption must be hazard-free, and an exemption
that no suite pins any more must be dropped.

**Both locale routes are swept, with every ceiling at zero.**
`/components/ai-chat` and `/components/ai-chat-es` joined `layout_audit_smoke`
and `style_audit_smoke` (ldui-iilm.11), each with its own `audit_test!` /
`style_audit_test!` invocation -- the macro takes an INDEX, so a `PAGES` entry
alone sweeps nothing and both suites stay green while appearing to cover the
page. Every ratcheted family is committed at ZERO pending measurement: a
ceiling is measured, and a plausible-looking number written before the lane ran
would absorb whatever the first sweep finds. The ES route is swept as a page in
its own right, because localized copy changes line counts and that is where a
layout regression hides.

## Recent additions (2026-09-15, later: Office deltas upstreamed)

4iiz-Office carried 22 files of vendor-only changes to this crate, which
blocked its re-vendor (and so blocked it adopting `Helpdesk`). All of them are
now upstream: **`EntityAutoFilters`** (the framework-built filter row) with
`EntityColumn::filterable*()`, **`EntityRowActions`** + `row_actions`,
**`SectionGrid`**, `DatasetSelector`/`SnapshotDatasetSelectorConfig` `compact`,
**`AutoPageSettle`**, `KpiStrip` card-shell overflow, `PageHeader` subtitle
gating, the `notebook` icon, `ResultList` `text-base-content/75`,
**`ModalSize::Large`**, and `TimelineList` `id`/`action_label`/`on_activate`.

**Upstream a consumer's deltas by three-way merge against THAT CONSUMER'S
re-vendor base, never by copying its files.** The base is the vendored tree as
it stood when it last synced (here 4iiz-Office `e9c0f6e7`), not the ldui commit
it synced from: merging against the latter replays deltas this repo has since
re-implemented differently, which turned 2 conflicts into 11. Five of the
declared deltas needed nothing at all.

Two traps the wave exposed, both invisible to every gate here:

- **A consumer checking a surface with `--all-targets` never compiles this
  crate's `cfg(test)` module**, so a vendored test can be broken for weeks. One
  captured `Rc<RefCell<_>>` in a `Callback::new`, which needs `Send + Sync`.
- **Clippy only lints what its target compiles.** `#[cfg(target_arch =
  "wasm32")]` code was never linted here, hiding six real lints. `verify` now
  carries a `clippy-lib-wasm` step.

Also: the style audit declares `EntityColumn::identifier()`'s mono role
(`mono_selectors`, PixelProof 7a34745) and daisyUI's resting control shadows,
instead of ratcheting them — see `tests/style_audit_smoke.rs` and note that a
ceiling's *justification* expires silently (those DEPTH ceilings cited a
PixelProof bug fixed two weeks earlier).

## Recent additions (2026-09-15, Helpdesk composite)

`patterns::Helpdesk` (ldui-b5mu) is the Jira-backed support desk: buckets,
filtered table, triage drawer, and a New Request dialog built on the new
`ImageAttachmentField`. **It owns no transport** -- every call goes through a
host-implemented `HelpdeskBackend` trait, and a `configured: false` meta renders
a panel with the submit button *absent*, not disabled. Roles are the pure
`RoleCapabilities::for_role` table; a requester's list asks the backend for
`Mine` and is never a hidden `All`, but the role is a rendering contract, not a
security boundary. `SegmentedBar` is a proportion bar, not a picker -- the kind
selector is radio-as-button. See
[`doc/components/helpdesk.md`](./doc/components/helpdesk.md).

Proven by `cargo xtask test-helpdesk` (9 tests, both roles on one document).
Test-host fixtures are selected by **pathname suffix**
(`/helpdesk-fixture-fail-writes`), never `?query`: `harness_at` appends its
own `?pp-freeze=1`, and a `/helpdesk-fixture?fault=…` navigation never mounted
the freeze stylesheet (60 s timeout). Follow the existing host's pathname idiom. Its first real execution found a product
defect no native test could: `capture_document_paste` ingested a paste inside
the drop zone **twice** (zone handler, then the bubbled window listener). The
guard checks event-target containment, not only `defaultPrevented` -- a
`cancelable: false` event ignores `preventDefault()`, so a flag-only guard
passed review and still failed the lane. It also surfaced two contrast traps
worth knowing: `NAME_PALETTE`'s `bg-primary` avatar pair measures 4.12:1 at
`Sm`, and a `RecordBadge` left at the default/`Neutral` soft tone measures
1.22:1 -- both are axe blockers wherever they appear. And an `aria-label` that
satisfies axe still fails the style audit's `input-outside-field` drift rule:
a filter control needs a real `<label>` (visually hidden text is fine).

## Recent additions (2026-09-02, later: SnapshotTablePage filter actions)

`SnapshotTablePage` grows an optional `filter_actions:
SnapshotFilterActionsConfig` (ldui-nj3q). Supplied, the `filters` slot composes
the framework `FilterBar` around the consumer's own content, yielding the
localized `{visible} of {total} results` count, one Reset, and one explicit
Save as Default. Absent, the slot renders exactly as before.

**A consumer of the composite must never compose `FilterBar` beside it** --
that reintroduces the page-local furniture the opinionated contract exists to
remove. Reach for `filter_actions` instead.

The config carries **no counts**. Visible comes from the identity-bound
`LocalResultSummary` minted by `state`, total from the authoritative displayed
snapshot, both read by the page -- so a consumer cannot pair a count with the
wrong generation, exactly as the selector and table configs already forbid.
The result string reuses `FilterBar`'s own `result_count` template, so one
page's copy cannot drift from another's. Save feedback renders in `FilterBar`'s
own live region rather than through `ActionFeedback`, which is keyed by the
page's action-key type `K` that a save has no way to mint.

Proven by `cargo xtask test-snapshot-table-page-filter-actions`, whose fixture
mounts **both** configurations on one document so every positive assertion
carries its negative control on the same run. See
[`doc/patterns/client-snapshot-list.md`](./doc/patterns/client-snapshot-list.md).

## Recent additions (2026-09-02, consumer-audit bead wave + comprehensive validation)

Eight beads cleared in one session; every fix carries a browser proof in a
registered lane, and the full 34-step gate plus the screenshot suite were run
green on the final tree (single-threaded against a detached release server,
because the shared box was under another session's load -- see
`doc/ci-cd.md`). What landed, and the trap behind each:

- **RecordHeader edge tooltips** (ldui-q73d) -- placement is re-measured on
  every reflow via a `ResizeObserver` on the section. A one-shot mount-time
  measurement is a photograph of first-paint geometry: the harness mounts at
  Chrome's 800px default and applies the viewport afterwards, so the first
  attempt measured the *stacked* layout and never re-measured.
- **Collapse toggle identity** (ldui-3k00) -- `id`/`name`/`aria_label` props;
  the input is named by its `CollapseTitle` through `CollapseContext`.
- **EntityTable** -- empty-state/chooser text and the compact-row field
  labels at the AA-passed 75% step (ldui-usqz; the 60% labels were 45 axe
  findings per narrow-viewport page); the scroll region is `tabindex=0` only
  when rows carry no roving stop (ldui-0bwg).
- **`.ld-card-depth` composes with Tailwind rings** (ldui-xr7i). A bare
  `box-shadow:` on a component class beats any layered `ring-*` utility and
  silently discards the ring; the class now sets `--tw-shadow` and the
  five-part list like Tailwind's own `shadow-*`. `SelectableSummaryCard`'s
  selection ring is an `outline` (a box-shadow ring counts as a rogue DEPTH
  finding), and keyboard focus is the framework's `ld-focus-ring`, which
  outranks any `focus-visible:` utility you might add.
- **`test-ld-class-coverage` is a native gate step** (ldui-n1iv). `test-lib`
  is `--lib` only; an integration test not named as its own step runs nowhere.
- **DataTable column tracks** (ldui-qsqz) -- a column that declares no width
  is an *auto* track (no `<col>` width, 96px floor toward the scroll minimum);
  ten undeclared columns fit `w-full` again instead of forcing 1600px.
- **Screenshot baselines** for modal-open and tab-default recaptured
  (ldui-fx46); they predated shipped fixtures.

Two hazards of a shared workstation, recorded in memory: a sibling session
that kills `cargo`/`trunk`/`xtask` machine-wide and races on `demo/dist`
(serve from a dist outside the repo; run suites single-threaded; a runner
that restarts the server before each suite), and `demo/node_modules`
vanishing mid-session (the stylesheet-stamp panic at
`tests/common/mod.rs:273` is the signature; `npm install` in `demo/`).

## Recent additions (2026-08-25, merged to main)

The interactive-chart epic (ldui-9tr), the 4iiz-etl portal bead wave, and a
previously in-flight a11y lane, all merged and verified (native gate 14/14 +
all three browser suites + visual 14/14 on the merged tree):

- **`LineChart` categorical mode is now fully interactive** (ldui-9tr):
  pointer hover cards, roving keyboard focus (Arrow/Home/End/Escape), typed
  `LineChartActivation` on click/Enter/Space (targets are `role="button"`
  only when a callback is wired; the interactive SVG is `role="group"` —
  `role="img"` makes focusable children presentational, an axe blocker),
  measured/clamped tooltip, ResizeObserver-driven tick thinning, and
  reconciliation-by-key across data changes. Legacy `Vec<(f64,f64)>` callers
  compile unchanged. The reactivity suite now carries a **vendored axe-core
  zero-blocking gate** (`tests/vendor/axe-core/`) and D1 browser-error
  capture seams in `tests/common`.
- **`Gauge`** (ldui-nx5) — 240-degree arc dial with warn/error budget bands
  at threshold fractions, zone-escalating value arc, tabular-nums readout;
  `role=meter`; paints routed via `charts::paint`.
- **`PhaseProgress`** (ldui-ymf) — equal phase segments, partial current
  fill, failed tone; `role=progressbar` with phase+pct valuetext.
- **`Pressable`** + **`TableViewport`** — unstyled action primitive and the
  shared horizontal-scroll viewport (`TABLE_VIEWPORT_CLASS`, aliased by
  DataTable's wrapper), landed with an AA-contrast pass (muted text at 75%
  base-content; `opacity-60`/`/50` variants fail axe).
- **DataTable `on_row_inspect`** (ldui-tmr) — dblclick/Shift+Enter secondary
  activation with `detail > 1` click swallowing so `on_row_activate` fires
  exactly once per double-click; **`ServerDataTable` forwards both
  callbacks** (ldui-1gp, page-local indices).
- **Timer/unmount safety** (ldui-d54) — the auto-rows measure timer and both
  search debounces are cancelled in `on_cleanup` and their late closures
  degrade to no-ops (try-forms) instead of panicking the wasm app; pinned by
  `data_table_timers_survive_unmount`. If you add a
  `set_timeout_with_handle` whose closure touches component signals, follow
  that pattern: store the handle, clear it in `on_cleanup`, try-forms in the
  closure.

## Recent additions (2026-08-10, consumer-requested bead wave)

Nine beads filed by the 4iiz-Office audit, cleared in one pass. Each was
verified against this repo's code before implementing -- four arrived as
duplicate pairs cross-filed hours apart, and one described a gap in the wrong
component.

- **`RosterGrid`** (new, `src/components/roster_grid/`) -- worker x weekday
  shift matrix. Deliberately NOT WeekView/DayScheduler, which are
  time-calendars (hours down one axis); a roster is a grid of worker x weekday
  where a cell carries a shift plus a state. Real `<table>` with `scope`
  headers, `ShiftState` colour PLUS a solid/dashed accent bar so the state is
  not colour-only, ragged rows padded by a pure tested `normalize_cells`, and
  ARIA-grid roving focus (one tab stop, arrows/Home/End, `role="grid"` only
  when interactive).
- **`Progress`** -- reactive `value`/`max`. Unset stays *indeterminate*: the
  attribute is genuinely omitted, not emitted as `value=0`. Do NOT mix these
  with `attr:value`/`attr:max`; the component writes its own `max`, and a
  duplicate resolves opposite ways on the CSR and `to_html` paths.
- **`BarChart` `bar_colors`**, **`Heatmap` `HeatScale::Judgement`** (signed
  intensity, theme-token hues, sense per-cell so one grid can mix
  higher-is-better and lower-is-better measures).
- **`widgets::DataTable` `row_actions`** -- a per-row action column.
  ⚠ Note there are TWO DataTables and consumers pick by accident; both doc
  comments now say which to reach for.
- **`FilterSidebar` `side`** -- `SidebarSide::{Left,Right}` for right-hand
  panels.
- **`src/charts/paint.rs`** -- `var()` inside an SVG *presentation attribute*
  is not specified to substitute, so a theme token there degrades to
  `fill: black` or `stroke: none` **silently**. Every chart and Gantt SVG paint
  now routes through it; `tests/svg_paint_routing.rs` scans all of `src/` and
  is a gate step.

## Recent additions (2026-08-06, from the office-perf audit)

All driven by 4iiz-Office consumer findings (op-99t7/op-cy77.x/op-rrp9); the
audit files beads into this repo's tracker continuously — check `bd list`.

- **DataTable**: owned `Column.header` + `Signal<DataTableTexts>` (runtime
  localization); `row_key` (selection keyed by row identity, survives data
  replacement and sorts); `Column::action()` (action cells never trigger
  `on_row_activate`); `extra_filter` predicate + `toolbar` ViewFn slot;
  column-scoped free-text search (`Column::searched(false)` opt-out —
  renderer-only metadata never matches); `ServerDataTable` typed
  `TableQuery`/`on_query_change` API (page/size/search/sort/filters) with
  `filter_options` for population-wide dropdowns. Both variants share
  `TABLE_SCROLL_WRAPPER_CLASS` (horizontal overflow), fixed column tracks, the
  dark-blue/light-blue semantic bands, and the faint full-cell grid. Consumers
  must import `styles/tokens.css` and scan this crate's `src/**/*.rs` in their
  own Tailwind build; demo CSS is not a delivery mechanism.
- **DayScheduler**: opt-in interaction contract — `on_event_activate`,
  `selected_event`, `on_event_move`/`on_event_resize` (keyboard Arrow /
  Shift+Arrow minute-delta requests; consumer owns the events),
  `event_content` renderer. Event blocks are index-keyed so focus survives
  moves.
- **Modal**: `label` / `labelled_by` / `described_by` accessible naming
  (`labelled_by` suppresses `aria-label`).
- **Field**: mints ids + provides `FieldContext`; `Input`/`Select`/`Textarea`
  auto-consume it (`label[for]`, `aria-describedby`, `aria-errormessage` +
  `aria-invalid`).
- **login_screen**: `ProviderLoginScreen` + `LoginProvider` — branded
  server-redirect OAuth landing (no credential state).
- **widgets**: `name_color_class`/`NAME_PALETTE` deterministic avatar
  palette (`AvatarBadge name=`, `Persona palette=true`); assignments pinned
  by test — changing the hash is a breaking visual change.
- **utils**: `use_event_source` (owned-lifecycle SSE, no `Closure::forget`)
  and `use_event_source_fetch` (authenticated SSE over fetch: headers
  callback per (re)connect, pure `SseParser`, `retry:`/`Last-Event-ID`).
- **LinkButton**: `href` is `MaybeProp<String>` (per-row routes).

## Recent additions (2026-07)
- **Spacing & vertical-rhythm system (2026-07-26).** `styles/tokens.css` is
  generated from `ui-tokens` and imported by `demo/input.css`, so Tailwind's
  numeric spacing scale and type ramp are derived from the same tokens the
  desktop face uses rather than merely agreeing with them. `src/tokens/preamble.rs`
  additionally emits `--ld-space-*`, `--ld-stroke-*`, `--ld-radius-*` and
  `--ld-text-*`/`--ld-line-*` at runtime, plus a `.ld-text-<step>` class per
  ramp step pinning both size and line height. `tests/layout_audit_smoke.rs`
  asserts overlap / grid / internal≤external over the rendered DOM. See the
  spacing rules under Component Guidelines and
  [`doc/plans/2026-07-26-spacing-audit.md`](./doc/plans/2026-07-26-spacing-audit.md).
- `sparkline/`, `empty_state/`, `icon_tile/`, `metric_row/`, `capacity_bar/`, `sla_chip/`, `nav_rail/`, `result_list/`, `day_scheduler/`, `toolbar/`, `tree/`, `week_view/` - new app-shell/data/scheduling components
- `day_scheduler` / `week_view` `hour_label` - optional
  `Callback<u32, String>` formatting the hour-gutter labels, overriding
  `hour_format` when supplied. `hour_format` (`HourFormat::TwentyFour` by
  default, `Twelve` on request) still covers the common case and mirrors
  d2d-ui's own two-variant enum; `hour_label` is the escape hatch for a locale
  neither format expresses. A localised consumer usually wants the *former* —
  driving `hour_format` off the active locale — so reach for the closure only
  when 24h and English AM/PM are both wrong.
- `vertical_steps/` - extended with additional layout options
- `src/motion/` - new animation module (see above)
- `src/utils/swr.rs` - stale-while-revalidate keyed resource cache
  (`use_swr_resource` / `SwrCache` / `provide_swr_cache`). Renders the cached value
  for a key instantly while a background fetch revalidates, so back-navigation
  doesn't spinner over data the user just saw. Demo: `/components/swr`.
- `data_table/auto_page.rs` + the `auto_page_size` prop - responsive paging: rows
  per page derived from the table's rendered height via a `ResizeObserver`.
- `data_table/filter.rs` + `Column::filterable()` - opt-in per-column filter row
  of dropdowns, ANDed with each other and with the existing `searchable` box.
- `data_table` `on_row_activate` + `row_click_kind()`/`RowClickKind` - opt-in row
  activation: a plain click activates (navigate/act on the row), a Ctrl/Shift
  click still feeds the selection state machine. With no callback registered,
  every click selects exactly as before.
- `data_table` keyboard operability + `row_is_interactive()` - when a table is
  interactive (`selected_rows` or `on_row_activate` supplied), rows are
  focusable (`tabindex=0`) with `aria-selected`, and Enter/Space (plus
  Ctrl/Shift) mirror a click. Plain display tables gain no tab stops. The
  internal `on_row_click` callback carries `(idx, ctrl, shift)` bools rather
  than a `MouseEvent` so mouse and keyboard share one path.
